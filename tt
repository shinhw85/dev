	<select id="nextTxSeq" resultType="int">
		/* api-sender.nextTxSeq : eai 트랜잭션 시퀀스 조회 */
		INSERT INTO eai_tx_seq (seq_date, seq)
		VALUES (CURRENT_DATE, 1)
		ON CONFLICT (seq_date)
		DO UPDATE SET seq = eai_tx_seq.seq + 1
		RETURNING seq
	</select>
